## Workspace Assets

Pasta para arquivos locais de trabalho (rascunhos, capturas, vídeos brutos, imagens de referência).

- Use `workspace-assets/raw/` para mídias temporárias.
- Conteúdo aqui não faz parte da aplicação em produção.
- O `.gitignore` está configurado para não versionar esses arquivos.
